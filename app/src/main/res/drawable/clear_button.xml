<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- Pressed state -->
    <item
        android:state_pressed="true"
        android:drawable="@color/colorClearBackgroundSelected"/>

    <!-- Focused state -->
    <item
        android:state_focused="true"
        android:drawable="@color/colorFocusedBackground"/>

    <!-- default state -->
    <item
        android:drawable="@color/colorClearBackground"/>


</selector>