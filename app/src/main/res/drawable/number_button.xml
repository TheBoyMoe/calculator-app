<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed state -->
    <item
        android:state_pressed="true"
        android:drawable="@color/colorButtonBackgroundSelected"/>

    <!-- Focused state -->
    <item
        android:state_focused="true"
        android:drawable="@color/colorFocusedBackground"/>

    <!-- Default state -->
    <item
        android:drawable="@color/colorButtonBackground"/>

</selector>